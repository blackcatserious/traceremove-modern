    </div>
    
    <!-- Footer -->
    <footer class="bg-gray-900 text-white">
        <div class="container mx-auto px-4 py-12">
            <div class="grid md:grid-cols-4 gap-8">
                <!-- Company Info -->
                <div class="col-span-1">
                    <div class="flex items-center space-x-2 mb-4">
                        <img src="/assets/images/logo.svg" alt="TraceRemove" class="h-8 w-auto">
                        <span class="text-xl font-bold">TraceRemove</span>
                    </div>
                    <p class="text-gray-400 mb-4">Advanced AI-powered reputation management and digital firewall protection for businesses and individuals.</p>
                    <div class="flex space-x-4">
                        <a href="https://linkedin.com/company/traceremove" class="text-gray-400 hover:text-white transition-colors">
                            <svg class="h-5 w-5" fill="currentColor" viewBox="0 0 24 24">
                                <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
                            </svg>
                        </a>
                        <a href="https://twitter.com/traceremove" class="text-gray-400 hover:text-white transition-colors">
                            <svg class="h-5 w-5" fill="currentColor" viewBox="0 0 24 24">
                                <path d="M23.953 4.57a10 10 0 01-2.825.775 4.958 4.958 0 002.163-2.723c-.951.555-2.005.959-3.127 1.184a4.92 4.92 0 00-8.384 4.482C7.69 8.095 4.067 6.13 1.64 3.162a4.822 4.822 0 00-.666 2.475c0 1.71.87 3.213 2.188 4.096a4.904 4.904 0 01-2.228-.616v.06a4.923 4.923 0 003.946 4.827 4.996 4.996 0 01-2.212.085 4.936 4.936 0 004.604 3.417 9.867 9.867 0 01-6.102 2.105c-.39 0-.779-.023-1.17-.067a13.995 13.995 0 007.557 2.209c9.053 0 13.998-7.496 13.998-13.985 0-.21 0-.42-.015-.63A9.935 9.935 0 0024 4.59z"/>
                            </svg>
                        </a>
                    </div>
                </div>
                
                <!-- Services -->
                <div>
                    <h3 class="text-lg font-semibold mb-4">Services</h3>
                    <ul class="space-y-2 text-gray-400">
                        <li><a href="/services#orm" class="hover:text-white transition-colors">Online Reputation Management</a></li>
                        <li><a href="/services#deindex" class="hover:text-white transition-colors">Content Deindexing</a></li>
                        <li><a href="/services#legal" class="hover:text-white transition-colors">Legal Support</a></li>
                        <li><a href="/services#monitoring" class="hover:text-white transition-colors">Brand Monitoring</a></li>
                        <li><a href="/services#ai-tools" class="hover:text-white transition-colors">AI Tools</a></li>
                    </ul>
                </div>
                
                <!-- Resources -->
                <div>
                    <h3 class="text-lg font-semibold mb-4">Resources</h3>
                    <ul class="space-y-2 text-gray-400">
                        <li><a href="/blog" class="hover:text-white transition-colors">Blog</a></li>
                        <li><a href="/cases" class="hover:text-white transition-colors">Success Cases</a></li>
                        <li><a href="/testimonials" class="hover:text-white transition-colors">Client Testimonials</a></li>
                        <li><a href="/blog/reputation-management-fundamentals.php" class="hover:text-white transition-colors">Guides</a></li>
                        <li><a href="/blog/technology-trends-reputation.php" class="hover:text-white transition-colors">Industry Insights</a></li>
                        <li><a href="/contact" class="hover:text-white transition-colors">Support</a></li>
                    </ul>
                </div>
                
                <!-- Contact -->
                <div>
                    <h3 class="text-lg font-semibold mb-4">Contact</h3>
                    <ul class="space-y-2 text-gray-400">
                        <li>
                            <a href="mailto:info@traceremove.com" class="hover:text-white transition-colors">
                                info@traceremove.com
                            </a>
                        </li>
                        <li>
                            <a href="tel:+1-800-TRACE-RM" class="hover:text-white transition-colors">
                                +1-800-TRACE-RM
                            </a>
                        </li>
                        <li class="flex items-center space-x-2">
                            <a href="https://wa.me/1234567890" class="flex items-center space-x-2 hover:text-white transition-colors">
                                <img src="/whatsapp-icon.svg" alt="WhatsApp" class="h-4 w-4">
                                <span>WhatsApp</span>
                            </a>
                        </li>
                        <li class="flex items-center space-x-2">
                            <a href="https://t.me/traceremove" class="flex items-center space-x-2 hover:text-white transition-colors">
                                <img src="/telegram-icon.svg" alt="Telegram" class="h-4 w-4">
                                <span>Telegram</span>
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
            
            <!-- Bottom Bar -->
            <div class="border-t border-gray-800 mt-8 pt-8 flex flex-col md:flex-row justify-between items-center">
                <p class="text-gray-400 text-sm">
                    © <?php echo date('Y'); ?> TraceRemove. All rights reserved.
                </p>
                <div class="flex space-x-6 mt-4 md:mt-0">
                    <a href="/privacy" class="text-gray-400 hover:text-white text-sm transition-colors">Privacy Policy</a>
                    <a href="/terms" class="text-gray-400 hover:text-white text-sm transition-colors">Terms of Service</a>
                    <a href="/cookies" class="text-gray-400 hover:text-white text-sm transition-colors">Cookie Policy</a>
                </div>
            </div>
        </div>
    </footer>
    
    <!-- Scripts -->
    <script>
        document.getElementById('mobile-menu-button').addEventListener('click', function() {
            const mobileMenu = document.getElementById('mobile-menu');
            mobileMenu.classList.toggle('hidden');
        });
        
        function initThemeToggle() {
            const themeToggleBtn = document.getElementById('theme-toggle');
            const mobileThemeToggleBtn = document.getElementById('mobile-theme-toggle');
            const themeToggleDarkIcon = document.getElementById('theme-toggle-dark-icon');
            const themeToggleLightIcon = document.getElementById('theme-toggle-light-icon');
            const mobileThemeToggleDarkIcon = document.getElementById('mobile-theme-toggle-dark-icon');
            const mobileThemeToggleLightIcon = document.getElementById('mobile-theme-toggle-light-icon');
            
            const currentTheme = localStorage.getItem('theme') || 'light';
            
            function updateIcons(theme) {
                if (theme === 'dark') {
                    themeToggleDarkIcon?.classList.add('hidden');
                    themeToggleLightIcon?.classList.remove('hidden');
                    mobileThemeToggleDarkIcon?.classList.add('hidden');
                    mobileThemeToggleLightIcon?.classList.remove('hidden');
                } else {
                    themeToggleDarkIcon?.classList.remove('hidden');
                    themeToggleLightIcon?.classList.add('hidden');
                    mobileThemeToggleDarkIcon?.classList.remove('hidden');
                    mobileThemeToggleLightIcon?.classList.add('hidden');
                }
            }
            
            updateIcons(currentTheme);
            
            function toggleTheme() {
                const currentTheme = localStorage.getItem('theme') || 'light';
                const newTheme = currentTheme === 'light' ? 'dark' : 'light';
                
                localStorage.setItem('theme', newTheme);
                
                if (newTheme === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
                
                updateIcons(newTheme);
                
                gsap.to('body', { duration: 0.3, ease: "power2.out" });
            }
            
            themeToggleBtn?.addEventListener('click', toggleTheme);
            mobileThemeToggleBtn?.addEventListener('click', toggleTheme);
        }
        
        document.addEventListener('DOMContentLoaded', initThemeToggle);
        
        gsap.registerPlugin(ScrollTrigger);
        
        gsap.timeline()
            .fromTo('.hero-title', 
                { opacity: 0, y: 100, scale: 0.8 },
                { opacity: 1, y: 0, scale: 1, duration: 1.2, ease: "power3.out" }
            )
            .fromTo('.hero-subtitle', 
                { opacity: 0, y: 50 },
                { opacity: 1, y: 0, duration: 0.8, ease: "power2.out" }, "-=0.6"
            )
            .fromTo('.hero-buttons', 
                { opacity: 0, y: 30, scale: 0.9 },
                { opacity: 1, y: 0, scale: 1, duration: 0.6, ease: "back.out(1.7)" }, "-=0.4"
            );
        
        document.querySelectorAll('.btn-premium').forEach(button => {
            button.addEventListener('mouseenter', () => {
                gsap.to(button, { scale: 1.05, duration: 0.3, ease: "power2.out" });
            });
            button.addEventListener('mouseleave', () => {
                gsap.to(button, { scale: 1, duration: 0.3, ease: "power2.out" });
            });
        });
        
        gsap.utils.toArray('.service-card').forEach((card, index) => {
            gsap.fromTo(card,
                { opacity: 0, y: 60, rotationY: 15 },
                {
                    opacity: 1,
                    y: 0,
                    rotationY: 0,
                    duration: 0.8,
                    delay: index * 0.1,
                    ease: "power3.out",
                    scrollTrigger: {
                        trigger: card,
                        start: "top 85%",
                        toggleActions: "play none none reverse"
                    }
                }
            );
        });
        
        gsap.utils.toArray('.fade-in').forEach((element) => {
            gsap.fromTo(element, 
                { opacity: 0, y: 60, scale: 0.95 },
                {
                    opacity: 1,
                    y: 0,
                    scale: 1,
                    duration: 1.2,
                    ease: "power3.out",
                    scrollTrigger: {
                        trigger: element,
                        start: "top 85%",
                        end: "bottom 15%",
                        toggleActions: "play none none reverse"
                    }
                }
            );
        });
        
        gsap.utils.toArray('.scale-in').forEach((element) => {
            gsap.fromTo(element,
                { scale: 0.7, opacity: 0, rotation: -5 },
                {
                    scale: 1,
                    opacity: 1,
                    rotation: 0,
                    duration: 0.8,
                    ease: "back.out(1.7)",
                    scrollTrigger: {
                        trigger: element,
                        start: "top 85%",
                        end: "bottom 15%",
                        toggleActions: "play none none reverse"
                    }
                }
            );
        });
        
        gsap.utils.toArray('.stagger-in').forEach((container) => {
            const items = container.querySelectorAll('.stagger-item');
            gsap.fromTo(items,
                { opacity: 0, y: 40, scale: 0.9, rotation: 2 },
                {
                    opacity: 1,
                    y: 0,
                    scale: 1,
                    rotation: 0,
                    duration: 0.7,
                    ease: "power2.out",
                    stagger: 0.15,
                    scrollTrigger: {
                        trigger: container,
                        start: "top 80%",
                        end: "bottom 20%",
                        toggleActions: "play none none reverse"
                    }
                }
            );
        });
        
        gsap.utils.toArray('.counter').forEach((counter) => {
            const target = parseInt(counter.getAttribute('data-target'));
            const isPercentage = counter.textContent.includes('%');
            const isTime = counter.textContent.includes('/');
            const hasPlus = counter.textContent.includes('+');
            
            gsap.fromTo(counter, 
                { textContent: 0 },
                {
                    textContent: target,
                    duration: 2,
                    ease: "power2.out",
                    snap: { textContent: 1 },
                    onUpdate: function() {
                        const currentValue = Math.ceil(this.targets()[0].textContent);
                        if (isPercentage) {
                            counter.textContent = currentValue + '%';
                        } else if (isTime) {
                            counter.textContent = currentValue + '/7';
                        } else if (hasPlus && currentValue >= 1000) {
                            counter.textContent = (currentValue / 1000).toFixed(0) + ',000+';
                        } else if (hasPlus) {
                            counter.textContent = currentValue + '+';
                        } else {
                            counter.textContent = currentValue;
                        }
                    },
                    scrollTrigger: {
                        trigger: counter,
                        start: "top 85%",
                        toggleActions: "play none none reverse"
                    }
                }
            );
        });
        
        const testimonialsSlider = document.querySelector('.testimonials-slider');
        if (testimonialsSlider) {
            const track = testimonialsSlider.querySelector('.testimonials-track');
            const slides = testimonialsSlider.querySelectorAll('.testimonial-slide');
            const prevBtn = testimonialsSlider.querySelector('.testimonial-prev');
            const nextBtn = testimonialsSlider.querySelector('.testimonial-next');
            const dots = testimonialsSlider.querySelectorAll('.testimonial-dot');
            
            let currentSlide = 0;
            const totalSlides = slides.length;
            let autoplayInterval;
            
            function updateSlider(slideIndex, animate = true) {
                const translateX = -slideIndex * 100;
                
                if (animate) {
                    gsap.to(track, {
                        x: `${translateX}%`,
                        duration: 0.6,
                        ease: "power2.inOut"
                    });
                } else {
                    gsap.set(track, { x: `${translateX}%` });
                }
                
                dots.forEach((dot, index) => {
                    if (index === slideIndex) {
                        gsap.to(dot, { backgroundColor: '#2563eb', scale: 1.2, duration: 0.3 });
                    } else {
                        gsap.to(dot, { backgroundColor: '#d1d5db', scale: 1, duration: 0.3 });
                    }
                });
                
                slides.forEach((slide, index) => {
                    const content = slide.querySelector('.bg-white');
                    if (index === slideIndex) {
                        gsap.fromTo(content, 
                            { opacity: 0.7, y: 20 },
                            { opacity: 1, y: 0, duration: 0.8, ease: "power2.out", delay: 0.2 }
                        );
                    }
                });
            }
            
            function nextSlide() {
                currentSlide = (currentSlide + 1) % totalSlides;
                updateSlider(currentSlide);
            }
            
            function prevSlide() {
                currentSlide = (currentSlide - 1 + totalSlides) % totalSlides;
                updateSlider(currentSlide);
            }
            
            function goToSlide(slideIndex) {
                currentSlide = slideIndex;
                updateSlider(currentSlide);
            }
            
            function startAutoplay() {
                autoplayInterval = setInterval(nextSlide, 5000); // Change slide every 5 seconds
            }
            
            function stopAutoplay() {
                if (autoplayInterval) {
                    clearInterval(autoplayInterval);
                }
            }
            
            nextBtn.addEventListener('click', () => {
                nextSlide();
                stopAutoplay();
                setTimeout(startAutoplay, 10000); // Restart autoplay after 10 seconds
            });
            
            prevBtn.addEventListener('click', () => {
                prevSlide();
                stopAutoplay();
                setTimeout(startAutoplay, 10000);
            });
            
            dots.forEach((dot, index) => {
                dot.addEventListener('click', () => {
                    goToSlide(index);
                    stopAutoplay();
                    setTimeout(startAutoplay, 10000);
                });
            });
            
            testimonialsSlider.addEventListener('mouseenter', stopAutoplay);
            testimonialsSlider.addEventListener('mouseleave', startAutoplay);
            
            let startX = 0;
            let isDragging = false;
            
            testimonialsSlider.addEventListener('touchstart', (e) => {
                startX = e.touches[0].clientX;
                isDragging = true;
                stopAutoplay();
            });
            
            testimonialsSlider.addEventListener('touchmove', (e) => {
                if (!isDragging) return;
                e.preventDefault();
            });
            
            testimonialsSlider.addEventListener('touchend', (e) => {
                if (!isDragging) return;
                isDragging = false;
                
                const endX = e.changedTouches[0].clientX;
                const diffX = startX - endX;
                
                if (Math.abs(diffX) > 50) { // Minimum swipe distance
                    if (diffX > 0) {
                        nextSlide();
                    } else {
                        prevSlide();
                    }
                }
                
                setTimeout(startAutoplay, 10000);
            });
            
            updateSlider(0, false);
            
            ScrollTrigger.create({
                trigger: testimonialsSlider,
                start: "top 80%",
                onEnter: startAutoplay,
                onLeave: stopAutoplay
            });
        }
        
        const contactForm = document.getElementById('contact-form');
        if (contactForm) {
            const inputs = contactForm.querySelectorAll('input[type="text"], input[type="email"], input[type="tel"], select, textarea');
            const submitBtn = document.getElementById('submit-btn');
            const submitText = document.getElementById('submit-text');
            const submitLoading = document.getElementById('submit-loading');
            
            inputs.forEach(input => {
                const label = contactForm.querySelector(`label[for="${input.id}"]`);
                
                input.addEventListener('focus', () => {
                    gsap.to(input, {
                        scale: 1.02,
                        borderColor: '#3b82f6',
                        boxShadow: '0 0 0 3px rgba(59, 130, 246, 0.1)',
                        duration: 0.3,
                        ease: "power2.out"
                    });
                    
                    if (label) {
                        gsap.to(label, {
                            color: '#3b82f6',
                            scale: 1.05,
                            duration: 0.3,
                            ease: "power2.out"
                        });
                    }
                });
                
                input.addEventListener('blur', () => {
                    gsap.to(input, {
                        scale: 1,
                        borderColor: '#d1d5db',
                        boxShadow: '0 0 0 0px rgba(59, 130, 246, 0)',
                        duration: 0.3,
                        ease: "power2.out"
                    });
                    
                    if (label) {
                        gsap.to(label, {
                            color: '#374151',
                            scale: 1,
                            duration: 0.3,
                            ease: "power2.out"
                        });
                    }
                });
                
                input.addEventListener('input', () => {
                    if (input.value.length > 0) {
                        gsap.to(input, {
                            backgroundColor: '#f8fafc',
                            duration: 0.2
                        });
                    } else {
                        gsap.to(input, {
                            backgroundColor: '#ffffff',
                            duration: 0.2
                        });
                    }
                });
            });
            
            if (submitBtn) {
                submitBtn.addEventListener('mouseenter', () => {
                    gsap.to(submitBtn, {
                        scale: 1.05,
                        boxShadow: '0 10px 25px rgba(59, 130, 246, 0.3)',
                        duration: 0.3,
                        ease: "power2.out"
                    });
                });
                
                submitBtn.addEventListener('mouseleave', () => {
                    gsap.to(submitBtn, {
                        scale: 1,
                        boxShadow: '0 4px 15px rgba(59, 130, 246, 0.2)',
                        duration: 0.3,
                        ease: "power2.out"
                    });
                });
                
                submitBtn.addEventListener('mousedown', () => {
                    gsap.to(submitBtn, {
                        scale: 0.98,
                        duration: 0.1
                    });
                });
                
                submitBtn.addEventListener('mouseup', () => {
                    gsap.to(submitBtn, {
                        scale: 1.05,
                        duration: 0.1
                    });
                });
                
                contactForm.addEventListener('submit', (e) => {
                    if (submitText && submitLoading) {
                        gsap.to(submitText, {
                            opacity: 0,
                            y: -10,
                            duration: 0.3,
                            onComplete: () => {
                                submitText.classList.add('hidden');
                                submitLoading.classList.remove('hidden');
                                gsap.fromTo(submitLoading, 
                                    { opacity: 0, y: 10 },
                                    { opacity: 1, y: 0, duration: 0.3 }
                                );
                            }
                        });
                    }
                    
                    gsap.to(submitBtn, {
                        scale: 1.1,
                        duration: 0.2,
                        yoyo: true,
                        repeat: 1,
                        ease: "power2.inOut"
                    });
                    
                    submitBtn.disabled = true;
                    gsap.to(submitBtn, {
                        opacity: 0.8,
                        duration: 0.3
                    });
                });
            }
            
            const showValidationError = (input, message) => {
                let errorElement = input.parentNode.querySelector('.validation-error');
                if (!errorElement) {
                    errorElement = document.createElement('div');
                    errorElement.className = 'validation-error text-red-600 text-sm mt-1';
                    input.parentNode.appendChild(errorElement);
                }
                
                errorElement.textContent = message;
                
                gsap.to(input, {
                    borderColor: '#ef4444',
                    boxShadow: '0 0 0 3px rgba(239, 68, 68, 0.1)',
                    duration: 0.3
                });
                
                gsap.fromTo(errorElement, 
                    { opacity: 0, y: -10 },
                    { opacity: 1, y: 0, duration: 0.3 }
                );
                
                gsap.to(input, {
                    x: -5,
                    duration: 0.1,
                    yoyo: true,
                    repeat: 5,
                    ease: "power2.inOut"
                });
            };
            
            const hideValidationError = (input) => {
                const errorElement = input.parentNode.querySelector('.validation-error');
                if (errorElement) {
                    gsap.to(errorElement, {
                        opacity: 0,
                        y: -10,
                        duration: 0.3,
                        onComplete: () => errorElement.remove()
                    });
                }
                
                gsap.to(input, {
                    borderColor: '#d1d5db',
                    boxShadow: '0 0 0 0px rgba(239, 68, 68, 0)',
                    duration: 0.3
                });
            };
            
            inputs.forEach(input => {
                input.addEventListener('blur', () => {
                    if (input.hasAttribute('required') && !input.value.trim()) {
                        showValidationError(input, 'This field is required');
                    } else if (input.type === 'email' && input.value && !input.value.match(/^[^\s@]+@[^\s@]+\.[^\s@]+$/)) {
                        showValidationError(input, 'Please enter a valid email address');
                    } else {
                        hideValidationError(input);
                    }
                });
                
                input.addEventListener('input', () => {
                    if (input.parentNode.querySelector('.validation-error')) {
                        hideValidationError(input);
                    }
                });
            });
        }
        
        const successMessage = document.querySelector('.bg-green-50');
        const errorMessage = document.querySelector('.bg-red-50');
        
        if (successMessage) {
            gsap.fromTo(successMessage, 
                { opacity: 0, scale: 0.9, y: 20 },
                { opacity: 1, scale: 1, y: 0, duration: 0.6, ease: "back.out(1.7)" }
            );
        }
        
        if (errorMessage) {
            gsap.fromTo(errorMessage, 
                { opacity: 0, scale: 0.9, y: 20 },
                { opacity: 1, scale: 1, y: 0, duration: 0.6, ease: "back.out(1.7)" }
            );
        }
        
        const nav = document.querySelector('nav');
        const mobileMenuButton = document.getElementById('mobile-menu-button');
        const mobileMenu = document.getElementById('mobile-menu');
        const themeToggle = document.getElementById('theme-toggle');
        const mobileThemeToggle = document.getElementById('mobile-theme-toggle');
        const navLinks = document.querySelectorAll('nav a:not(.btn-premium)');
        
        if (nav) {
            let lastScrollY = window.scrollY;
            
            window.addEventListener('scroll', () => {
                const currentScrollY = window.scrollY;
                
                if (currentScrollY > 100) {
                    gsap.to(nav, {
                        backgroundColor: 'rgba(255, 255, 255, 0.98)',
                        backdropFilter: 'blur(12px)',
                        boxShadow: '0 4px 20px rgba(0, 0, 0, 0.1)',
                        duration: 0.3,
                        ease: "power2.out"
                    });
                } else {
                    gsap.to(nav, {
                        backgroundColor: 'rgba(255, 255, 255, 0.95)',
                        backdropFilter: 'blur(8px)',
                        boxShadow: '0 1px 3px rgba(0, 0, 0, 0.05)',
                        duration: 0.3,
                        ease: "power2.out"
                    });
                }
                
                if (currentScrollY > lastScrollY && currentScrollY > 200) {
                    gsap.to(nav, {
                        y: -80,
                        duration: 0.3,
                        ease: "power2.inOut"
                    });
                } else {
                    gsap.to(nav, {
                        y: 0,
                        duration: 0.3,
                        ease: "power2.inOut"
                    });
                }
                
                lastScrollY = currentScrollY;
            });
        }
        
        navLinks.forEach(link => {
            const originalText = link.textContent;
            
            link.addEventListener('mouseenter', () => {
                gsap.to(link, {
                    scale: 1.05,
                    color: '#2563eb',
                    duration: 0.3,
                    ease: "power2.out"
                });
                
                if (!link.querySelector('.nav-underline')) {
                    const underline = document.createElement('div');
                    underline.className = 'nav-underline absolute bottom-0 left-0 h-0.5 bg-blue-600';
                    underline.style.width = '0%';
                    link.style.position = 'relative';
                    link.appendChild(underline);
                    
                    gsap.to(underline, {
                        width: '100%',
                        duration: 0.3,
                        ease: "power2.out"
                    });
                }
            });
            
            link.addEventListener('mouseleave', () => {
                gsap.to(link, {
                    scale: 1,
                    color: '#374151',
                    duration: 0.3,
                    ease: "power2.out"
                });
                
                const underline = link.querySelector('.nav-underline');
                if (underline) {
                    gsap.to(underline, {
                        width: '0%',
                        duration: 0.3,
                        ease: "power2.out",
                        onComplete: () => underline.remove()
                    });
                }
            });
        });
        
        if (mobileMenuButton && mobileMenu) {
            let mobileMenuOpen = false;
            
            gsap.set(mobileMenu, {
                height: 0,
                opacity: 0,
                overflow: 'hidden'
            });
            
            mobileMenuButton.addEventListener('click', () => {
                const menuItems = mobileMenu.querySelectorAll('a, button');
                
                if (!mobileMenuOpen) {
                    mobileMenu.classList.remove('hidden');
                    
                    gsap.to(mobileMenu, {
                        height: 'auto',
                        opacity: 1,
                        duration: 0.4,
                        ease: "power2.out"
                    });
                    
                    gsap.fromTo(menuItems, 
                        { opacity: 0, x: -20 },
                        { 
                            opacity: 1, 
                            x: 0, 
                            duration: 0.3, 
                            stagger: 0.1,
                            delay: 0.2,
                            ease: "power2.out"
                        }
                    );
                    
                    const hamburgerLines = mobileMenuButton.querySelectorAll('path');
                    gsap.to(hamburgerLines[0], {
                        attr: { d: "M6 18L18 6" },
                        duration: 0.3
                    });
                    gsap.to(hamburgerLines[1], {
                        opacity: 0,
                        duration: 0.3
                    });
                    gsap.to(hamburgerLines[2], {
                        attr: { d: "M6 6l12 12" },
                        duration: 0.3
                    });
                    
                    mobileMenuOpen = true;
                } else {
                    gsap.to(menuItems, {
                        opacity: 0,
                        x: -20,
                        duration: 0.2,
                        stagger: 0.05
                    });
                    
                    gsap.to(mobileMenu, {
                        height: 0,
                        opacity: 0,
                        duration: 0.3,
                        delay: 0.2,
                        ease: "power2.inOut",
                        onComplete: () => {
                            mobileMenu.classList.add('hidden');
                        }
                    });
                    
                    const hamburgerLines = mobileMenuButton.querySelectorAll('path');
                    gsap.to(hamburgerLines[0], {
                        attr: { d: "M4 6h16" },
                        duration: 0.3
                    });
                    gsap.to(hamburgerLines[1], {
                        opacity: 1,
                        duration: 0.3
                    });
                    gsap.to(hamburgerLines[2], {
                        attr: { d: "M4 18h16" },
                        duration: 0.3
                    });
                    
                    mobileMenuOpen = false;
                }
            });
            
            const mobileLinks = mobileMenu.querySelectorAll('a');
            mobileLinks.forEach(link => {
                link.addEventListener('click', () => {
                    if (mobileMenuOpen) {
                        mobileMenuButton.click();
                    }
                });
            });
        }
        
        const setupThemeToggle = (toggleButton, darkIcon, lightIcon) => {
            if (!toggleButton) return;
            
            const darkIconEl = document.getElementById(darkIcon);
            const lightIconEl = document.getElementById(lightIcon);
            
            const isDark = document.documentElement.classList.contains('dark');
            if (isDark) {
                if (lightIconEl) lightIconEl.classList.remove('hidden');
                if (darkIconEl) darkIconEl.classList.add('hidden');
            } else {
                if (darkIconEl) darkIconEl.classList.remove('hidden');
                if (lightIconEl) lightIconEl.classList.add('hidden');
            }
            
            toggleButton.addEventListener('click', () => {
                const currentlyDark = document.documentElement.classList.contains('dark');
                
                gsap.to(toggleButton, {
                    scale: 1.2,
                    duration: 0.1,
                    yoyo: true,
                    repeat: 1,
                    ease: "power2.inOut"
                });
                
                if (currentlyDark) {
                    document.documentElement.classList.remove('dark');
                    localStorage.setItem('theme', 'light');
                    
                    if (lightIconEl && darkIconEl) {
                        gsap.to(lightIconEl, {
                            opacity: 0,
                            rotation: 180,
                            duration: 0.3,
                            onComplete: () => {
                                lightIconEl.classList.add('hidden');
                                darkIconEl.classList.remove('hidden');
                                gsap.fromTo(darkIconEl, 
                                    { opacity: 0, rotation: -180 },
                                    { opacity: 1, rotation: 0, duration: 0.3 }
                                );
                            }
                        });
                    }
                } else {
                    document.documentElement.classList.add('dark');
                    localStorage.setItem('theme', 'dark');
                    
                    if (darkIconEl && lightIconEl) {
                        gsap.to(darkIconEl, {
                            opacity: 0,
                            rotation: 180,
                            duration: 0.3,
                            onComplete: () => {
                                darkIconEl.classList.add('hidden');
                                lightIconEl.classList.remove('hidden');
                                gsap.fromTo(lightIconEl, 
                                    { opacity: 0, rotation: -180 },
                                    { opacity: 1, rotation: 0, duration: 0.3 }
                                );
                            }
                        });
                    }
                }
            });
            
            toggleButton.addEventListener('mouseenter', () => {
                gsap.to(toggleButton, {
                    scale: 1.1,
                    backgroundColor: 'rgba(59, 130, 246, 0.1)',
                    duration: 0.3,
                    ease: "power2.out"
                });
            });
            
            toggleButton.addEventListener('mouseleave', () => {
                gsap.to(toggleButton, {
                    scale: 1,
                    backgroundColor: 'transparent',
                    duration: 0.3,
                    ease: "power2.out"
                });
            });
        };
        
        setupThemeToggle(themeToggle, 'theme-toggle-dark-icon', 'theme-toggle-light-icon');
        setupThemeToggle(mobileThemeToggle, 'mobile-theme-toggle-dark-icon', 'mobile-theme-toggle-light-icon');
        
        const socialIcons = document.querySelectorAll('footer a[href*="linkedin"], footer a[href*="twitter"], footer a[href*="wa.me"], footer a[href*="t.me"]');
        
        socialIcons.forEach(icon => {
            const svg = icon.querySelector('svg');
            const img = icon.querySelector('img');
            const iconElement = svg || img;
            
            if (iconElement) {
                gsap.set(iconElement, {
                    transformOrigin: "center center"
                });
                
                icon.addEventListener('mouseenter', () => {
                    gsap.to(iconElement, {
                        scale: 1.3,
                        rotation: 10,
                        duration: 0.3,
                        ease: "back.out(1.7)"
                    });
                    
                    gsap.to(icon, {
                        color: '#ffffff',
                        duration: 0.3,
                        ease: "power2.out"
                    });
                    
                    gsap.to(icon, {
                        textShadow: '0 0 10px rgba(59, 130, 246, 0.5)',
                        duration: 0.3,
                        ease: "power2.out"
                    });
                    
                    gsap.to(iconElement, {
                        scale: 1.4,
                        duration: 0.6,
                        yoyo: true,
                        repeat: -1,
                        ease: "power2.inOut"
                    });
                });
                
                icon.addEventListener('mouseleave', () => {
                    gsap.killTweensOf(iconElement);
                    
                    gsap.to(iconElement, {
                        scale: 1,
                        rotation: 0,
                        duration: 0.4,
                        ease: "back.out(1.7)"
                    });
                    
                    gsap.to(icon, {
                        color: '#9ca3af',
                        textShadow: '0 0 0px rgba(59, 130, 246, 0)',
                        duration: 0.3,
                        ease: "power2.out"
                    });
                });
                
                icon.addEventListener('mousedown', () => {
                    gsap.to(iconElement, {
                        scale: 0.9,
                        duration: 0.1,
                        ease: "power2.inOut"
                    });
                });
                
                icon.addEventListener('mouseup', () => {
                    gsap.to(iconElement, {
                        scale: 1.3,
                        duration: 0.2,
                        ease: "back.out(2)"
                    });
                });
            }
        });
        
        const footerLinks = document.querySelectorAll('footer a:not([href*="linkedin"]):not([href*="twitter"]):not([href*="wa.me"]):not([href*="t.me"])');
        
        footerLinks.forEach(link => {
            link.addEventListener('mouseenter', () => {
                gsap.to(link, {
                    x: 5,
                    color: '#ffffff',
                    duration: 0.3,
                    ease: "power2.out"
                });
            });
            
            link.addEventListener('mouseleave', () => {
                gsap.to(link, {
                    x: 0,
                    color: '#9ca3af',
                    duration: 0.3,
                    ease: "power2.out"
                });
            });
        });
        
        const premiumButtons = document.querySelectorAll('.btn-premium, .btn-premium-glow, .btn-premium-outline, .btn-premium-pulse');
        
        premiumButtons.forEach(button => {
            gsap.set(button, {
                transformOrigin: "center center"
            });
            
            button.addEventListener('mouseenter', () => {
                gsap.to(button, {
                    scale: 1.08,
                    duration: 0.4,
                    ease: "back.out(1.7)"
                });
                
                gsap.to(button, {
                    boxShadow: '0 0 30px rgba(102, 126, 234, 0.8), 0 0 60px rgba(118, 75, 162, 0.6), 0 16px 48px rgba(102, 126, 234, 0.4)',
                    duration: 0.4,
                    ease: "power2.out"
                });
                
                gsap.to(button, {
                    boxShadow: '0 0 40px rgba(102, 126, 234, 1), 0 0 80px rgba(118, 75, 162, 0.8), 0 20px 60px rgba(102, 126, 234, 0.6)',
                    duration: 1,
                    yoyo: true,
                    repeat: -1,
                    ease: "power2.inOut"
                });
                
                gsap.to(button, {
                    textShadow: '0 0 10px rgba(255, 255, 255, 0.8), 0 0 20px rgba(255, 255, 255, 0.6)',
                    duration: 0.3,
                    ease: "power2.out"
                });
                
                if (button.classList.contains('btn-premium') || button.classList.contains('btn-premium-glow')) {
                    gsap.to(button, {
                        backgroundPosition: '200% 200%',
                        duration: 2,
                        ease: "power2.inOut",
                        repeat: -1,
                        yoyo: true
                    });
                }
            });
            
            button.addEventListener('mouseleave', () => {
                gsap.killTweensOf(button);
                
                gsap.to(button, {
                    scale: 1,
                    boxShadow: '0 8px 32px rgba(102, 126, 234, 0.3)',
                    textShadow: '0 0 0px rgba(255, 255, 255, 0)',
                    backgroundPosition: '0% 0%',
                    duration: 0.4,
                    ease: "power2.out"
                });
            });
            
            button.addEventListener('mousedown', () => {
                gsap.to(button, {
                    scale: 0.95,
                    duration: 0.1,
                    ease: "power2.inOut"
                });
                
                const ripple = document.createElement('div');
                ripple.style.cssText = `
                    position: absolute;
                    border-radius: 50%;
                    background: rgba(255, 255, 255, 0.6);
                    transform: scale(0);
                    animation: ripple 0.6s linear;
                    pointer-events: none;
                    left: 50%;
                    top: 50%;
                    width: 20px;
                    height: 20px;
                    margin-left: -10px;
                    margin-top: -10px;
                `;
                
                button.style.position = 'relative';
                button.style.overflow = 'hidden';
                button.appendChild(ripple);
                
                gsap.fromTo(ripple, 
                    { scale: 0, opacity: 1 },
                    { 
                        scale: 4, 
                        opacity: 0, 
                        duration: 0.6,
                        ease: "power2.out",
                        onComplete: () => ripple.remove()
                    }
                );
            });
            
            button.addEventListener('mouseup', () => {
                gsap.to(button, {
                    scale: 1.08,
                    duration: 0.2,
                    ease: "back.out(2)"
                });
            });
            
            button.addEventListener('mousemove', (e) => {
                const rect = button.getBoundingClientRect();
                const x = e.clientX - rect.left - rect.width / 2;
                const y = e.clientY - rect.top - rect.height / 2;
                
                gsap.to(button, {
                    x: x * 0.1,
                    y: y * 0.1,
                    duration: 0.3,
                    ease: "power2.out"
                });
            });
            
            button.addEventListener('mouseleave', () => {
                gsap.to(button, {
                    x: 0,
                    y: 0,
                    duration: 0.4,
                    ease: "back.out(1.7)"
                });
            });
        });
        
        const glowButtons = document.querySelectorAll('.btn-premium-glow');
        glowButtons.forEach(button => {
            gsap.to(button, {
                boxShadow: '0 0 25px rgba(102, 126, 234, 0.6), 0 8px 32px rgba(102, 126, 234, 0.4)',
                duration: 2,
                yoyo: true,
                repeat: -1,
                ease: "power2.inOut"
            });
        });
        
        const outlineButtons = document.querySelectorAll('.btn-premium-outline');
        outlineButtons.forEach(button => {
            button.addEventListener('mouseenter', () => {
                gsap.to(button, {
                    borderColor: '#667eea',
                    color: '#667eea',
                    duration: 0.3,
                    ease: "power2.out"
                });
                
                gsap.fromTo(button, 
                    { 
                        backgroundImage: 'linear-gradient(white, white), linear-gradient(135deg, #667eea 0%, #764ba2 50%, #667eea 100%)',
                        backgroundSize: '100% 100%, 0% 100%'
                    },
                    {
                        backgroundSize: '100% 100%, 100% 100%',
                        duration: 0.6,
                        ease: "power2.out"
                    }
                );
            });
        });
        
        const pulseButtons = document.querySelectorAll('.btn-premium-pulse');
        pulseButtons.forEach(button => {
            gsap.to(button, {
                scale: 1.05,
                duration: 1,
                yoyo: true,
                repeat: -1,
                ease: "power2.inOut"
            });
            
            gsap.to(button, {
                boxShadow: '0 0 20px rgba(102, 126, 234, 0.8), 0 8px 32px rgba(102, 126, 234, 0.4)',
                duration: 1.5,
                yoyo: true,
                repeat: -1,
                ease: "power2.inOut"
            });
        });
        
        gsap.registerPlugin(ScrollTrigger);
        
        gsap.utils.toArray('.fade-in').forEach((element) => {
            gsap.fromTo(element, 
                { 
                    opacity: 0, 
                    y: 50 
                },
                {
                    opacity: 1,
                    y: 0,
                    duration: 1,
                    ease: "power2.out",
                    scrollTrigger: {
                        trigger: element,
                        start: "top 85%",
                        end: "bottom 15%",
                        toggleActions: "play none none reverse"
                    }
                }
            );
        });
        
        gsap.utils.toArray('.stagger-in').forEach((container) => {
            const items = container.querySelectorAll('.stagger-item');
            
            gsap.fromTo(items, 
                { 
                    opacity: 0, 
                    y: 30,
                    scale: 0.9
                },
                {
                    opacity: 1,
                    y: 0,
                    scale: 1,
                    duration: 0.6,
                    ease: "back.out(1.7)",
                    stagger: 0.1,
                    scrollTrigger: {
                        trigger: container,
                        start: "top 80%",
                        end: "bottom 20%",
                        toggleActions: "play none none reverse"
                    }
                }
            );
        });
        
        gsap.utils.toArray('.scale-in').forEach((element) => {
            gsap.fromTo(element, 
                { 
                    opacity: 0, 
                    scale: 0.8,
                    rotation: -5
                },
                {
                    opacity: 1,
                    scale: 1,
                    rotation: 0,
                    duration: 0.8,
                    ease: "back.out(1.7)",
                    scrollTrigger: {
                        trigger: element,
                        start: "top 85%",
                        end: "bottom 15%",
                        toggleActions: "play none none reverse"
                    }
                }
            );
        });
        
        gsap.utils.toArray('.slide-in-left').forEach((element) => {
            gsap.fromTo(element, 
                { 
                    opacity: 0, 
                    x: -100
                },
                {
                    opacity: 1,
                    x: 0,
                    duration: 1,
                    ease: "power3.out",
                    scrollTrigger: {
                        trigger: element,
                        start: "top 85%",
                        end: "bottom 15%",
                        toggleActions: "play none none reverse"
                    }
                }
            );
        });
        
        gsap.utils.toArray('.slide-in-right').forEach((element) => {
            gsap.fromTo(element, 
                { 
                    opacity: 0, 
                    x: 100
                },
                {
                    opacity: 1,
                    x: 0,
                    duration: 1,
                    ease: "power3.out",
                    scrollTrigger: {
                        trigger: element,
                        start: "top 85%",
                        end: "bottom 15%",
                        toggleActions: "play none none reverse"
                    }
                }
            );
        });
        
        gsap.utils.toArray('.bounce-in').forEach((element) => {
            gsap.fromTo(element, 
                { 
                    opacity: 0, 
                    y: -50,
                    scale: 0.3
                },
                {
                    opacity: 1,
                    y: 0,
                    scale: 1,
                    duration: 1.2,
                    ease: "bounce.out",
                    scrollTrigger: {
                        trigger: element,
                        start: "top 85%",
                        end: "bottom 15%",
                        toggleActions: "play none none reverse"
                    }
                }
            );
        });
        
        gsap.utils.toArray('.rotate-in').forEach((element) => {
            gsap.fromTo(element, 
                { 
                    opacity: 0, 
                    rotation: 180,
                    scale: 0.5
                },
                {
                    opacity: 1,
                    rotation: 0,
                    scale: 1,
                    duration: 1,
                    ease: "back.out(1.7)",
                    scrollTrigger: {
                        trigger: element,
                        start: "top 85%",
                        end: "bottom 15%",
                        toggleActions: "play none none reverse"
                    }
                }
            );
        });
        
        gsap.utils.toArray('.text-reveal').forEach((element) => {
            const chars = element.textContent.split('');
            element.innerHTML = chars.map(char => `<span style="display: inline-block; opacity: 0;">${char === ' ' ? '&nbsp;' : char}</span>`).join('');
            
            const spans = element.querySelectorAll('span');
            
            gsap.to(spans, {
                opacity: 1,
                duration: 0.05,
                stagger: 0.02,
                ease: "power2.out",
                scrollTrigger: {
                    trigger: element,
                    start: "top 85%",
                    end: "bottom 15%",
                    toggleActions: "play none none reverse"
                }
            });
        });
        
        gsap.utils.toArray('.parallax').forEach((element) => {
            gsap.to(element, {
                yPercent: -50,
                ease: "none",
                scrollTrigger: {
                    trigger: element,
                    start: "top bottom",
                    end: "bottom top",
                    scrub: true
                }
            });
        });
        
        gsap.utils.toArray('.section-reveal').forEach((section) => {
            gsap.fromTo(section, 
                { 
                    opacity: 0,
                    backgroundColor: "rgba(0,0,0,0.05)"
                },
                {
                    opacity: 1,
                    backgroundColor: "rgba(0,0,0,0)",
                    duration: 1.5,
                    ease: "power2.out",
                    scrollTrigger: {
                        trigger: section,
                        start: "top 80%",
                        end: "bottom 20%",
                        toggleActions: "play none none reverse"
                    }
                }
            );
        });
        
        const progressBar = document.createElement('div');
        progressBar.style.cssText = `
            position: fixed;
            top: 0;
            left: 0;
            width: 0%;
            height: 4px;
            background: linear-gradient(90deg, #667eea, #764ba2);
            z-index: 9999;
            transition: width 0.1s ease;
        `;
        document.body.appendChild(progressBar);
        
        gsap.to(progressBar, {
            width: "100%",
            ease: "none",
            scrollTrigger: {
                trigger: "body",
                start: "top top",
                end: "bottom bottom",
                scrub: 0.3
            }
        });
        
        
        const loadingOverlay = document.createElement('div');
        loadingOverlay.id = 'loading-overlay';
        loadingOverlay.style.cssText = `
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, #667eea, #764ba2);
            z-index: 99999;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
        `;
        
        const loadingSpinner = document.createElement('div');
        loadingSpinner.style.cssText = `
            width: 60px;
            height: 60px;
            border: 4px solid rgba(255, 255, 255, 0.3);
            border-top: 4px solid white;
            border-radius: 50%;
            animation: spin 1s linear infinite;
            margin-bottom: 20px;
        `;
        
        const loadingText = document.createElement('div');
        loadingText.textContent = 'TraceRemove';
        loadingText.style.cssText = `
            color: white;
            font-size: 24px;
            font-weight: bold;
            text-align: center;
        `;
        
        loadingOverlay.appendChild(loadingSpinner);
        loadingOverlay.appendChild(loadingText);
        document.body.appendChild(loadingOverlay);
        
        const style = document.createElement('style');
        style.textContent = `
            @keyframes spin {
                0% { transform: rotate(0deg); }
                100% { transform: rotate(360deg); }
            }
            @keyframes fadeInUp {
                from {
                    opacity: 0;
                    transform: translateY(30px);
                }
                to {
                    opacity: 1;
                    transform: translateY(0);
                }
            }
            .fade-in-up {
                animation: fadeInUp 0.6s ease-out forwards;
            }
        `;
        document.head.appendChild(style);
        
        window.addEventListener('load', () => {
            gsap.to(loadingOverlay, {
                opacity: 0,
                duration: 0.8,
                ease: "power2.out",
                onComplete: () => {
                    loadingOverlay.remove();
                }
            });
            
            const timeline = gsap.timeline({ delay: 0.3 });
            
            timeline.fromTo('nav', 
                { y: -100, opacity: 0 },
                { y: 0, opacity: 1, duration: 0.6, ease: "back.out(1.7)" }
            );
            
            timeline.fromTo('.hero-section', 
                { scale: 0.9, opacity: 0 },
                { scale: 1, opacity: 1, duration: 0.8, ease: "power2.out" },
                "-=0.3"
            );
            
            timeline.fromTo('.hero-section h1', 
                { y: 50, opacity: 0 },
                { y: 0, opacity: 1, duration: 0.6, ease: "power2.out" },
                "-=0.4"
            );
            
            timeline.fromTo('.hero-section p', 
                { y: 30, opacity: 0 },
                { y: 0, opacity: 1, duration: 0.6, ease: "power2.out" },
                "-=0.3"
            );
            
            timeline.fromTo('.hero-buttons', 
                { y: 20, opacity: 0 },
                { y: 0, opacity: 1, duration: 0.6, ease: "power2.out" },
                "-=0.2"
            );
            
            timeline.fromTo('.section', 
                { y: 30, opacity: 0 },
                { 
                    y: 0, 
                    opacity: 1, 
                    duration: 0.6, 
                    ease: "power2.out",
                    stagger: 0.1
                },
                "-=0.2"
            );
        });
        
        document.querySelectorAll('a:not([href^="#"]):not([href^="mailto"]):not([href^="tel"]):not([target="_blank"])').forEach(link => {
            link.addEventListener('click', (e) => {
                const href = link.getAttribute('href');
                
                if (href.startsWith('http') || href.startsWith('//') || link.hasAttribute('download')) {
                    return;
                }
                
                e.preventDefault();
                
                const transitionOverlay = document.createElement('div');
                transitionOverlay.style.cssText = `
                    position: fixed;
                    top: 0;
                    left: 0;
                    width: 100%;
                    height: 100%;
                    background: linear-gradient(135deg, #667eea, #764ba2);
                    z-index: 99998;
                    opacity: 0;
                    pointer-events: none;
                `;
                document.body.appendChild(transitionOverlay);
                
                gsap.to(transitionOverlay, {
                    opacity: 1,
                    duration: 0.4,
                    ease: "power2.inOut",
                    onComplete: () => {
                        window.location.href = href;
                    }
                });
            });
        });
        
        window.addEventListener('pageshow', (e) => {
            if (e.persisted) {
                gsap.fromTo('body', 
                    { opacity: 0 },
                    { opacity: 1, duration: 0.3, ease: "power2.out" }
                );
            }
        });
        
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };
        
        const contentObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    const element = entry.target;
                    
                    element.classList.add('fade-in-up');
                    
                    contentObserver.unobserve(element);
                }
            });
        }, observerOptions);
        
        document.querySelectorAll('section, .card, .service-item, .testimonial, .pricing-card').forEach(el => {
            contentObserver.observe(el);
        });
        
        const preloadResources = [
            '/assets/css/traceremove_main.css',
            '/assets/images/logo.svg',
            '/assets/images/logo-white.svg'
        ];
        
        preloadResources.forEach(resource => {
            const link = document.createElement('link');
            link.rel = 'preload';
            link.as = resource.endsWith('.css') ? 'style' : 'image';
            link.href = resource;
            document.head.appendChild(link);
        });
        
        let loadingProgress = 0;
        const progressIndicator = document.createElement('div');
        progressIndicator.style.cssText = `
            position: fixed;
            bottom: 20px;
            right: 20px;
            background: rgba(102, 126, 234, 0.9);
            color: white;
            padding: 10px 15px;
            border-radius: 20px;
            font-size: 12px;
            z-index: 99997;
            opacity: 0;
            pointer-events: none;
        `;
        progressIndicator.textContent = 'Loading...';
        document.body.appendChild(progressIndicator);
        
        const progressTimeout = setTimeout(() => {
            gsap.to(progressIndicator, {
                opacity: 1,
                duration: 0.3,
                ease: "power2.out"
            });
        }, 1000);
        
        window.addEventListener('load', () => {
            clearTimeout(progressTimeout);
            gsap.to(progressIndicator, {
                opacity: 0,
                duration: 0.3,
                ease: "power2.out",
                onComplete: () => progressIndicator.remove()
            });
        });
        
        
        const isMobile = /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent) || window.innerWidth <= 768;
        
        if (isMobile) {
            document.querySelectorAll('.btn-premium, .btn-premium-glow, .btn-premium-outline, button, .card, .service-item').forEach(element => {
                element.addEventListener('touchstart', (e) => {
                    gsap.to(element, {
                        scale: 0.95,
                        duration: 0.1,
                        ease: "power2.out"
                    });
                    
                    const touch = e.touches[0];
                    const rect = element.getBoundingClientRect();
                    const ripple = document.createElement('div');
                    
                    ripple.style.cssText = `
                        position: absolute;
                        border-radius: 50%;
                        background: rgba(102, 126, 234, 0.3);
                        transform: scale(0);
                        pointer-events: none;
                        left: ${touch.clientX - rect.left}px;
                        top: ${touch.clientY - rect.top}px;
                        width: 20px;
                        height: 20px;
                        margin-left: -10px;
                        margin-top: -10px;
                    `;
                    
                    element.style.position = 'relative';
                    element.style.overflow = 'hidden';
                    element.appendChild(ripple);
                    
                    gsap.to(ripple, {
                        scale: 8,
                        opacity: 0,
                        duration: 0.6,
                        ease: "power2.out",
                        onComplete: () => ripple.remove()
                    });
                });
                
                element.addEventListener('touchend', () => {
                    gsap.to(element, {
                        scale: 1,
                        duration: 0.2,
                        ease: "back.out(1.7)"
                    });
                });
                
                element.addEventListener('touchcancel', () => {
                    gsap.to(element, {
                        scale: 1,
                        duration: 0.2,
                        ease: "power2.out"
                    });
                });
            });
            
            gsap.utils.toArray('.fade-in, .scale-in, .slide-in-left, .slide-in-right').forEach(element => {
                gsap.set(element, {
                    willChange: 'transform, opacity'
                });
                
                gsap.fromTo(element, 
                    { opacity: 0, y: 20 },
                    {
                        opacity: 1,
                        y: 0,
                        duration: 0.4,
                        ease: "power2.out",
                        scrollTrigger: {
                            trigger: element,
                            start: "top 90%",
                            end: "bottom 10%",
                            toggleActions: "play none none none"
                        }
                    }
                );
            });
            
            const mobileMenuToggle = document.querySelector('.mobile-menu-toggle');
            const mobileMenu = document.querySelector('.mobile-menu');
            
            if (mobileMenuToggle && mobileMenu) {
                mobileMenuToggle.addEventListener('touchstart', () => {
                    gsap.to(mobileMenuToggle, {
                        scale: 0.9,
                        duration: 0.1,
                        ease: "power2.out"
                    });
                });
                
                mobileMenuToggle.addEventListener('touchend', () => {
                    gsap.to(mobileMenuToggle, {
                        scale: 1,
                        duration: 0.2,
                        ease: "back.out(1.7)"
                    });
                });
                
                const menuItems = mobileMenu.querySelectorAll('a');
                mobileMenuToggle.addEventListener('click', () => {
                    if (mobileMenu.classList.contains('hidden')) {
                        mobileMenu.classList.remove('hidden');
                        gsap.fromTo(mobileMenu, 
                            { x: '100%', opacity: 0 },
                            { x: '0%', opacity: 1, duration: 0.3, ease: "power2.out" }
                        );
                        
                        gsap.fromTo(menuItems, 
                            { x: 50, opacity: 0 },
                            { 
                                x: 0, 
                                opacity: 1, 
                                duration: 0.2, 
                                stagger: 0.05,
                                delay: 0.1,
                                ease: "power2.out" 
                            }
                        );
                    } else {
                        gsap.to(mobileMenu, {
                            x: '100%',
                            opacity: 0,
                            duration: 0.3,
                            ease: "power2.in",
                            onComplete: () => mobileMenu.classList.add('hidden')
                        });
                    }
                });
            }
            
            const testimonialSlider = document.querySelector('.testimonials-slider');
            if (testimonialSlider) {
                let startX = 0;
                let currentX = 0;
                let isDragging = false;
                
                testimonialSlider.addEventListener('touchstart', (e) => {
                    startX = e.touches[0].clientX;
                    isDragging = true;
                    gsap.killTweensOf(testimonialSlider);
                });
                
                testimonialSlider.addEventListener('touchmove', (e) => {
                    if (!isDragging) return;
                    
                    currentX = e.touches[0].clientX;
                    const deltaX = currentX - startX;
                    
                    gsap.set(testimonialSlider, {
                        x: deltaX * 0.3
                    });
                });
                
                testimonialSlider.addEventListener('touchend', () => {
                    if (!isDragging) return;
                    isDragging = false;
                    
                    const deltaX = currentX - startX;
                    
                    if (Math.abs(deltaX) > 50) {
                        const direction = deltaX > 0 ? 'prev' : 'next';
                        const event = new CustomEvent('testimonialSwipe', { detail: { direction } });
                        testimonialSlider.dispatchEvent(event);
                    }
                    
                    gsap.to(testimonialSlider, {
                        x: 0,
                        duration: 0.3,
                        ease: "power2.out"
                    });
                });
            }
            
            document.querySelectorAll('input, textarea, select').forEach(input => {
                input.addEventListener('focus', () => {
                    gsap.to(input, {
                        scale: 1.02,
                        duration: 0.2,
                        ease: "power2.out"
                    });
                    
                    setTimeout(() => {
                        input.scrollIntoView({ 
                            behavior: 'smooth', 
                            block: 'center' 
                        });
                    }, 300);
                });
                
                input.addEventListener('blur', () => {
                    gsap.to(input, {
                        scale: 1,
                        duration: 0.2,
                        ease: "power2.out"
                    });
                });
            });
            
            document.addEventListener('DOMContentLoaded', () => {
                if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
                    gsap.globalTimeline.timeScale(0.5);
                }
                
                gsap.set('*', {
                    force3D: true
                });
            });
            
            const scrollIndicators = document.querySelectorAll('.scroll-indicator');
            scrollIndicators.forEach(indicator => {
                indicator.addEventListener('touchstart', () => {
                    gsap.to(indicator, {
                        scale: 1.2,
                        duration: 0.1,
                        ease: "power2.out"
                    });
                });
                
                indicator.addEventListener('touchend', () => {
                    gsap.to(indicator, {
                        scale: 1,
                        duration: 0.2,
                        ease: "back.out(1.7)"
                    });
                });
            });
            
        } else {
            document.querySelectorAll('.card, .service-item, .pricing-card').forEach(card => {
                card.addEventListener('mouseenter', () => {
                    gsap.to(card, {
                        y: -10,
                        scale: 1.02,
                        duration: 0.3,
                        ease: "power2.out"
                    });
                });
                
                card.addEventListener('mouseleave', () => {
                    gsap.to(card, {
                        y: 0,
                        scale: 1,
                        duration: 0.3,
                        ease: "power2.out"
                    });
                });
            });
        }
        
        window.addEventListener('resize', () => {
            ScrollTrigger.refresh();
            
            const newIsMobile = window.innerWidth <= 768;
            if (newIsMobile !== isMobile) {
                location.reload();
            }
        });
        
        window.addEventListener('orientationchange', () => {
            setTimeout(() => {
                ScrollTrigger.refresh();
            }, 500);
        });
        
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });
        
        const newsletterForm = document.querySelector('.newsletter-form');
        if (newsletterForm) {
            newsletterForm.addEventListener('submit', function(e) {
                e.preventDefault();
                const email = this.querySelector('input[type="email"]').value;
                
                if (!email || !email.includes('@')) {
                    alert('Please enter a valid email address.');
                    return;
                }
                
                alert('Thank you for subscribing! We\'ll keep you updated with the latest insights.');
                this.reset();
            });
        }
        
        const contactForm = document.querySelector('.contact-form');
        if (contactForm) {
            contactForm.addEventListener('submit', function(e) {
                const submitBtn = document.getElementById('submit-btn');
                const submitText = document.getElementById('submit-text');
                const submitLoading = document.getElementById('submit-loading');
                
                const requiredFields = this.querySelectorAll('[required]');
                let isValid = true;
                
                requiredFields.forEach(field => {
                    if (!field.value.trim()) {
                        isValid = false;
                        field.classList.add('border-red-500');
                        field.focus();
                    } else {
                        field.classList.remove('border-red-500');
                    }
                });
                
                const emailField = this.querySelector('input[type="email"]');
                if (emailField && emailField.value && !emailField.value.includes('@')) {
                    isValid = false;
                    emailField.classList.add('border-red-500');
                    emailField.focus();
                }
                
                if (!isValid) {
                    e.preventDefault();
                    
                    const errorDiv = document.createElement('div');
                    errorDiv.className = 'bg-red-50 border border-red-200 rounded-lg p-4 mb-6 text-red-700';
                    errorDiv.innerHTML = '<strong>Please correct the highlighted fields before submitting.</strong>';
                    
                    const existingError = this.querySelector('.bg-red-50');
                    if (existingError) {
                        existingError.remove();
                    }
                    
                    this.insertBefore(errorDiv, this.firstChild);
                    
                    setTimeout(() => {
                        if (errorDiv.parentNode) {
                            errorDiv.remove();
                        }
                    }, 5000);
                    
                    return;
                }
                
                if (submitBtn && submitText && submitLoading) {
                    submitBtn.disabled = true;
                    submitText.classList.add('hidden');
                    submitLoading.classList.remove('hidden');
                }
                
            });
            
            const requiredFields = contactForm.querySelectorAll('[required]');
            requiredFields.forEach(field => {
                field.addEventListener('blur', function() {
                    if (this.value.trim()) {
                        this.classList.remove('border-red-500');
                        this.classList.add('border-green-500');
                    } else {
                        this.classList.add('border-red-500');
                        this.classList.remove('border-green-500');
                    }
                });
                
                field.addEventListener('input', function() {
                    if (this.value.trim()) {
                        this.classList.remove('border-red-500');
                    }
                });
            });
            
            const emailField = contactForm.querySelector('input[type="email"]');
            if (emailField) {
                emailField.addEventListener('blur', function() {
                    if (this.value && this.value.includes('@') && this.value.includes('.')) {
                        this.classList.remove('border-red-500');
                        this.classList.add('border-green-500');
                    } else if (this.value) {
                        this.classList.add('border-red-500');
                        this.classList.remove('border-green-500');
                    }
                });
            }
        }
    </script>
    
    <!-- Analytics -->
    <script>
    </script>
</body>
</html>
